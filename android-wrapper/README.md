# UNIQUE BRAVE Android wrapper

This is a separate Android wrapper source for the same live UNIQUE BRAVE backend. It loads `https://brave-hub.onrender.com/`, so it does not create a second database or user store.

Build with Android Studio/Gradle when an APK is needed. The web app remains the canonical backend and database.
