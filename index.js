require('dotenv').config();
const express = require('express');
const cors = require('cors');
const bcrypt = require('bcrypt');
const crypto = require('crypto');
const nodemailer = require('nodemailer');
const path = require('path');
const db = require('./backend/database');

const app = express();
const PORT = process.env.PORT || 3000;
const PUBLIC_BASE_URL = String(process.env.PUBLIC_BASE_URL || process.env.APP_URL || 'https://brave-hub.onrender.com').replace(/\/$/, '');
app.use(cors());
app.use(express.json({limit:'15mb'}));
app.use(express.urlencoded({extended:true, limit:'15mb'}));
app.use(express.static(path.join(__dirname,'public'), {index:false}));

const userSessions = new Map();
const adminSessions = new Map();
const loginFailures = new Map();
const aiResponses = {
  ng: [
"How far! I’m BRAVE AI. Tell me what you’re looking for and I’ll help you find it.","No wahala. I can help you search products, services or businesses on BRAVE.","Oga/ma, tell me the product name, colour, location or budget you have in mind.","You can use the Marketplace to compare available products and sellers.","If you need a service, tell me the kind of professional you want and your location.","I can help you turn a rough business idea into a simple plan.","For a company, BRAVE can organise products, staff, customers, records and business pages.","If a seller has the item you want, I can help you understand the listing before you contact them.","You can post a product with photos, delivery details and your preferred payment method.","You can post a service with your category, description and media.","Pay on delivery can be selected when a listing supports it.","Half payment plus delivery can be shown on a product listing where the seller chooses it.","A receipt can be created even when payment is not enabled; BRAVE can mark it as an order record.","Need a CV? Open BRAVE Workshop and choose CV Builder.","Need a letter? Open Letter Maker in the Workshop.","Need a poster? Use Poster Maker and then print or save the result.","Need a document? Document Maker can prepare a clean printable page.","Need an invoice? Invoice Maker is available in the Workshop.","Need a social caption? Social Media Maker can create a starting draft.","Need a Google Form plan? Form Planner can turn your questions into a clean structure.","Your Timeline works like a simple social feed: write a post, add a photo or video, and publish.","You can use your username as a public identity link on BRAVE.","A public profile link makes it easier to share your BRAVE page with customers.","For privacy, never post passwords, OTP codes or private financial details on your Timeline.","If you see suspicious content, use the report option so the admin can review it.","BRAVE’s admin can review flagged content and account-security events.","If you need a business page, open Company Centre from your dashboard.","Company Centre can be used to organise staff, customers, records and marketing ideas.","Your business profile can contain a business name, category, contact details and website.","You can copy your business link and share it outside BRAVE.","If you are a seller, make your product title clear and add useful photos.","If you are a service provider, explain what you actually do and who you serve.","A clear description helps customers understand a listing before they message the seller.","For Nigerian users, you can enter a local phone number beginning with 0; BRAVE normalises Nigerian numbers for account checks.","British English spelling is fine on BRAVE; the AI can understand it.","American English spelling is fine too; the AI can understand it.","You can ask me about availability, suitability, colours, categories or finding a seller.","If I cannot find an exact match, try a shorter product name or a category.","For example, instead of a long description, try ‘black office chair’.","For services, try terms such as ‘graphic design’, ‘typing’, ‘photo editing’ or ‘electrical repair’.","BRAVE is designed to connect buyers, sellers, professionals and businesses in one place."
  ],
  gb: [
"Hello! I’m BRAVE AI. Tell me what you’re looking for and I’ll help you search.","Certainly. I can help you find products, services or businesses on BRAVE.","Tell me the item, colour, location or price range you have in mind.","Use Marketplace to browse products and compare listings.","If you need a service, tell me what sort of professional you need and where you are.","I can help you organise a straightforward business plan.","Companies can use BRAVE to organise products, staff, customers, records and business pages.","I can help you understand a listing before you contact the seller.","A product listing can include photographs, delivery information and a payment option.","A service listing can include its category, description and media.","Pay on delivery can be displayed where the seller has selected it.","Half payment plus delivery can be displayed where the seller has selected it.","You can create a receipt without enabling payment and label it as an order record.","Open CV Builder in BRAVE Workshop if you need a CV.","Open Letter Maker if you need a formal letter.","Poster Maker can help you prepare a simple printable poster.","Document Maker can create a clean printable document.","Invoice Maker is available in the Workshop.","Social Media Maker can help you draft a promotional caption.","Form Planner can turn a list of questions into a useful form structure.","Timeline works like a straightforward social feed, with text, photographs and videos.","Your username can be used as a public BRAVE profile link.","A public profile link is useful when sharing your BRAVE presence with customers.","For safety, do not publish passwords, one-time codes or private financial information.","Report suspicious content so the BRAVE administrator can review it.","The administrator can review flagged content and security events.","Company Centre is the place for business-focused tools.","Company Centre can help organise staff, customers, records and marketing.","Your business profile can include your business name, category, contact details and website.","You can copy and share your business link outside BRAVE.","Clear product titles and useful photographs make listings easier to understand.","Service providers should explain what they offer and who they help.","A useful description gives customers more information before they send a message.","Nigerian telephone numbers beginning with 0 can be normalised for account checks.","British English is supported in BRAVE AI.","American English is supported in BRAVE AI as well.","Ask me about availability, suitability, colours, categories or seller matching.","If there is no exact match, try a shorter name or a broader category.","For example, try ‘black office chair’ rather than a long sentence.","For services, try ‘graphic design’, ‘typing’, ‘photo editing’ or ‘electrical repair’.","BRAVE brings buyers, sellers, professionals and businesses together."
  ],
  us: [
"Hi! I’m BRAVE AI. Tell me what you’re looking for and I’ll help you search.","Sure. I can help you find products, services, or businesses on BRAVE.","Tell me the item, color, location, or price range you have in mind.","Use Marketplace to browse products and compare listings.","If you need a service, tell me what kind of professional you need and your location.","I can help you organise a simple business plan.","Companies can use BRAVE to organize products, staff, customers, records, and business pages.","I can help you understand a listing before you contact the seller.","A product listing can include photos, delivery details, and a payment option.","A service listing can include its category, description, and media.","Pay on delivery can be displayed when the seller selects it.","Half payment plus delivery can be displayed when the seller selects it.","You can create a receipt without enabling payment and label it as an order record.","Open CV Builder in BRAVE Workshop if you need a CV.","Open Letter Maker if you need a formal letter.","Poster Maker can help you prepare a simple printable poster.","Document Maker can create a clean printable document.","Invoice Maker is available in the Workshop.","Social Media Maker can help you draft a promotional caption.","Form Planner can turn your questions into a useful form structure.","Timeline works like a simple social feed, with text, photos, and videos.","Your username can be used as a public BRAVE profile link.","A public profile link is useful for sharing your BRAVE presence with customers.","For safety, do not publish passwords, one-time codes, or private financial information.","Report suspicious content so the BRAVE administrator can review it.","The administrator can review flagged content and security events.","Company Centre is the place for business-focused tools.","Company Centre can help organize staff, customers, records, and marketing.","Your business profile can include your business name, category, contact details, and website.","You can copy and share your business link outside BRAVE.","Clear product titles and useful photos make listings easier to understand.","Service providers should explain what they offer and who they help.","A useful description gives customers more information before they send a message.","Nigerian phone numbers beginning with 0 can be normalized for account checks.","British English is supported in BRAVE AI.","American English is supported in BRAVE AI too.","Ask me about availability, suitability, colors, categories, or seller matching.","If there is no exact match, try a shorter name or a broader category.","For example, try ‘black office chair’ instead of a long sentence.","For services, try ‘graphic design’, ‘typing’, ‘photo editing’, or ‘electrical repair’.","BRAVE brings buyers, sellers, professionals, and businesses together."
  ]
};
const allResponses = [...aiResponses.ng,...aiResponses.gb,...aiResponses.us];

function id(prefix){return prefix+'_'+crypto.randomBytes(8).toString('hex');}
function now(){return new Date().toISOString();}
function clean(v){return String(v??'').trim();}
function email(v){return clean(v).toLowerCase();}
function username(v){return clean(v).toLowerCase().replace(/[^a-z0-9_]/g,'').slice(0,24);}
function phone(v){let x=clean(v).replace(/[^\d+]/g,''); if(x.startsWith('0')) x='+234'+x.slice(1); return x;}
function base64(v){return typeof v==='string' && v.length < 14000000 ? v : '';}
function safeUser(u){if(!u)return null;return {id:u.brave_id,username:u.username,fullname:u.fullname,email:u.email,phone:u.phone||'',country:u.country,profileImage:u.profile_image||'',accountStatus:u.account_status||'active',verificationStatus:u.verification_status||'unverified',emailVerified:!!u.email_verified,phoneVerified:!!u.phone_verified,createdAt:u.created_at};}
function currentUser(req){const t=clean((req.headers.authorization||'').replace(/^Bearer\s+/i,'')||req.headers['x-session-token']); const s=userSessions.get(t); if(!s)return null; return db.prepare('SELECT * FROM users WHERE brave_id=?').get(s.userId)||null;}
function requireUser(req,res,next){const u=currentUser(req);if(!u)return res.status(401).json({message:'Please log in to continue.'});if(u.account_status!=='active')return res.status(403).json({message:'Your account is not currently active.'});req.user=u;next();}
function requireAdmin(req,res,next){const t=clean(req.headers['x-admin-token']);if(!t||!adminSessions.has(t))return res.status(401).json({message:'Administrator authentication required.'});req.admin=true;next();}
function audit(action,targetType='',targetId='',details=''){db.prepare('INSERT INTO admin_audit(public_id,action,target_type,target_id,details) VALUES(?,?,?,?,?)').run(id('audit'),action,targetType,targetId,details);}
function security(req,eventType,userId='',details='',score=0){db.prepare('INSERT INTO security_events(public_id,user_id,event_type,ip,user_agent,details,risk_score) VALUES(?,?,?,?,?,?,?)').run(id('sec'),userId,eventType,req.ip,clean(req.get('user-agent')),details,score);}
function publicUrl(p){return PUBLIC_BASE_URL+p;}
function transporter(){if(!process.env.SMTP_HOST||!process.env.SMTP_USER||!process.env.SMTP_PASS)return null;return nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:Number(process.env.SMTP_PORT||587)===465,auth:{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}});}
async function sendMail(to,subject,html){const t=transporter();if(!t){console.error('SMTP configuration missing; recovery email not sent.');return {sent:false,error:'SMTP configuration missing'};}try{await t.sendMail({from:process.env.SMTP_FROM||process.env.SMTP_USER,to,subject,html});console.log('Recovery email sent successfully.');return {sent:true};}catch(e){console.error('SMTP email send failure:',e.message);return {sent:false,error:e.message};}}

app.get('/',(req,res)=>res.sendFile(path.join(__dirname,'public','home.html')));
app.get('/home',(req,res)=>res.sendFile(path.join(__dirname,'public','home.html')));
app.get('/login',(req,res)=>res.sendFile(path.join(__dirname,'public','login.html')));
app.get('/register',(req,res)=>res.sendFile(path.join(__dirname,'public','register.html')));
app.get('/admin-login',(req,res)=>res.sendFile(path.join(__dirname,'public','admin-login.html')));
app.get('/admin',(req,res)=>res.sendFile(path.join(__dirname,'public','admin.html')));
app.get('/u/:username',(req,res)=>res.sendFile(path.join(__dirname,'public','profile.html')));

app.get('/api/status',(req,res)=>res.json({status:'online',service:'Unique BRAVE',database:'connected',publicBaseUrl:PUBLIC_BASE_URL,twilio:'optional',aiResponses:allResponses.length,time:now()}));

app.post('/register',async(req,res)=>{
 try{
  const fullname=clean(req.body.fullname), uname=username(req.body.username||fullname.split(/\s+/)[0]), em=email(req.body.email), ph=phone(req.body.phone), country=clean(req.body.country)||'Nigeria', pw=String(req.body.password||'');
  if(!fullname||!uname||!em||!ph||pw.length<6)return res.status(400).json({message:'Please complete all required fields. Password must contain at least 6 characters.'});
  if(req.body.acceptedTerms!==true)return res.status(400).json({message:'You must accept the Terms & Conditions.'});
  if(db.prepare('SELECT 1 FROM users WHERE email=? OR username=? OR phone=?').get(em,uname,ph))return res.status(409).json({message:'Email, username or phone number is already in use.'});
  const hash=await bcrypt.hash(pw,10), uid=id('user');
  db.prepare(`INSERT INTO users(brave_id,username,fullname,email,country,phone,password_hash,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?)`).run(uid,uname,fullname,em,country,ph,hash,now(),now());
  db.prepare('INSERT INTO notifications(public_id,user_id,title,message) VALUES(?,?,?,?,?)'.replace('VALUES(?,?,?,?,?)','VALUES(?,?,?,?)')).run(id('note'),uid,'Welcome to Unique BRAVE','Your account has been created successfully.');
  audit('user_registered','user',uid,uname);
  res.status(201).json({message:'Account created successfully. Phone verification is optional until Twilio is configured.',user:safeUser(db.prepare('SELECT * FROM users WHERE brave_id=?').get(uid)),publicProfile:publicUrl('/u/'+uname)});
 }catch(e){console.error(e);res.status(500).json({message:'Unable to create your account right now.'});}
});

app.post('/login',async(req,res)=>{
 const identifier=email(req.body.identifier||req.body.email||req.body.phone), pw=String(req.body.password||'');
 if(req.body.acceptedTerms!==true)return res.status(400).json({message:'Please accept the Terms & Conditions before logging in.'});
 const key=identifier||'unknown', fails=loginFailures.get(key)||{count:0,at:0};
 const u=db.prepare('SELECT * FROM users WHERE email=? OR username=? OR phone=?').get(email(req.body.identifier||req.body.email),username(req.body.identifier||''),phone(req.body.identifier||req.body.phone));
 if(!u){fails.count++;loginFailures.set(key,fails);security(req,'failed_login','',`Unknown identifier: ${key}`,fails.count>=3?60:20);return res.status(401).json({message:'Invalid email, username, phone number or password.'});}
 if(u.account_status!=='active')return res.status(403).json({message:'This account is not currently active.'});
 const ok=await bcrypt.compare(pw,u.password_hash);
 if(!ok){fails.count++;loginFailures.set(key,fails);const score=Math.min(100,20+fails.count*20);security(req,'failed_login',u.brave_id,`Failed login attempt ${fails.count}`,score);return res.status(401).json({message:'Invalid email, username, phone number or password.'});}
 loginFailures.delete(key);const token=crypto.randomBytes(32).toString('hex');userSessions.set(token,{userId:u.brave_id,createdAt:Date.now()});security(req,'login_success',u.brave_id,'Successful login',0);res.json({message:'Login successful.',token,user:safeUser(u),publicProfile:publicUrl('/u/'+u.username)});
});
app.post('/logout',(req,res)=>{const t=clean((req.headers.authorization||'').replace(/^Bearer\s+/i,'')||req.headers['x-session-token']);userSessions.delete(t);res.json({message:'Logged out successfully.'});});
app.get('/api/me',requireUser,(req,res)=>res.json({user:safeUser(req.user),usernameLink:publicUrl('/u/'+req.user.username)}));
app.post('/api/account/profile',requireUser,(req,res)=>{const name=clean(req.body.fullname);if(!name)return res.status(400).json({message:'Name is required.'});db.prepare('UPDATE users SET fullname=?,updated_at=? WHERE brave_id=?').run(name,now(),req.user.brave_id);res.json({message:'Profile updated.',user:safeUser(db.prepare('SELECT * FROM users WHERE brave_id=?').get(req.user.brave_id))});});


app.post('/change-password',requireUser,async(req,res)=>{const current=String(req.body.currentPassword||''), next=String(req.body.newPassword||'');if(next.length<6)return res.status(400).json({message:'New password must contain at least 6 characters.'});if(!await bcrypt.compare(current,req.user.password_hash))return res.status(401).json({message:'Current password is incorrect.'});db.prepare('UPDATE users SET password_hash=?,updated_at=? WHERE brave_id=?').run(await bcrypt.hash(next,10),now(),req.user.brave_id);security(req,'password_changed',req.user.brave_id,'Password changed',0);res.json({message:'Password changed successfully.'});});

app.post('/api/account/change-request',requireUser,(req,res)=>{const type=clean(req.body.type), reason=clean(req.body.reason), requestedValue=clean(req.body.requestedValue);const allowed=['phone number','email address','verification method','profile image','account deletion'];if(!allowed.includes(type))return res.status(400).json({message:'Unsupported protected change.'});const pending=db.prepare('SELECT 1 FROM protected_requests WHERE user_id=? AND type=? AND status=?').get(req.user.brave_id,type,'pending');if(pending)return res.status(409).json({message:'You already have a pending request of this type.'});db.prepare('INSERT INTO protected_requests(public_id,user_id,type,reason,requested_value) VALUES(?,?,?,?,?)').run(id('request'),req.user.brave_id,type,reason,requestedValue);security(req,'protected_change_request',req.user.brave_id,type,15);res.status(201).json({message:'Request sent to admin for review.'});});

app.get('/api/products',(req,res)=>res.json(db.prepare("SELECT * FROM products WHERE status='active' ORDER BY featured DESC,id DESC").all().map(p=>({...p,publicUrl:publicUrl('/product/'+p.public_id)}))));
app.post('/api/products',requireUser,(req,res)=>{const name=clean(req.body.name||req.body.title);if(!name)return res.status(400).json({message:'Product name is required.'});const pid=id('product');db.prepare(`INSERT INTO products(public_id,owner_id,owner_name,owner_username,name,category,description,price,delivery_price,payment_method,image_data,video_data,featured,status) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).run(pid,req.user.brave_id,req.user.fullname,req.user.username,name,clean(req.body.category)||'General',clean(req.body.description),req.body.price===''?null:Number(req.body.price)||null,req.body.deliveryPrice===''?null:Number(req.body.deliveryPrice)||null,clean(req.body.paymentMethod)||'pay_on_delivery',base64(req.body.image),base64(req.body.video),0,'pending');db.prepare('INSERT INTO moderation(public_id,content_type,content_id,reason) VALUES(?,?,?,?)').run(id('moderation'),'product',pid,'New marketplace submission');res.status(201).json({message:'Product submitted for admin review.',product:db.prepare('SELECT * FROM products WHERE public_id=?').get(pid)});});
app.post('/api/admin/products',requireAdmin,(req,res)=>{const name=clean(req.body.name);if(!name)return res.status(400).json({message:'Product name is required.'});const pid=id('product');db.prepare(`INSERT INTO products(public_id,owner_id,owner_name,owner_username,name,category,description,price,delivery_price,payment_method,image_data,video_data,featured,status) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).run(pid,'ADMIN','BRAVE','brave',name,clean(req.body.category)||'Featured',clean(req.body.description),req.body.price===''?null:Number(req.body.price)||null,req.body.deliveryPrice===''?null:Number(req.body.deliveryPrice)||null,clean(req.body.paymentMethod)||'pay_on_delivery',base64(req.body.image),base64(req.body.video),1,'active');audit('admin_product_posted','product',pid,name);res.status(201).json({message:'Admin product posted.',product:db.prepare('SELECT * FROM products WHERE public_id=?').get(pid)});});
app.get('/product/:id',(req,res)=>res.redirect('/marketplace.html?product='+encodeURIComponent(req.params.id)));

app.get('/api/services',(req,res)=>res.json(db.prepare("SELECT * FROM services WHERE status='active' ORDER BY id DESC").all()));
app.post('/api/services',requireUser,(req,res)=>{const name=clean(req.body.name||req.body.title);if(!name)return res.status(400).json({message:'Service name is required.'});const sid=id('service');db.prepare(`INSERT INTO services(public_id,owner_id,owner_name,owner_username,name,category,description,price,image_data,video_data,status) VALUES(?,?,?,?,?,?,?,?,?,?,?)`).run(sid,req.user.brave_id,req.user.fullname,req.user.username,name,clean(req.body.category)||'Professional service',clean(req.body.description),req.body.price===''?null:Number(req.body.price)||null,base64(req.body.image),base64(req.body.video),'pending');db.prepare('INSERT INTO moderation(public_id,content_type,content_id,reason) VALUES(?,?,?,?)').run(id('moderation'),'service',sid,'New service submission');res.status(201).json({message:'Service submitted for admin review.',service:db.prepare('SELECT * FROM services WHERE public_id=?').get(sid)});});

app.get('/api/timeline/feed', (req,res)=>res.json(db.prepare("SELECT * FROM timeline_posts WHERE status='active' ORDER BY id DESC LIMIT 100").all()));
app.get('/api/timeline/:userId',(req,res)=>res.json(db.prepare("SELECT * FROM timeline_posts WHERE user_id=? AND status='active' ORDER BY id DESC").all(req.params.userId)));
app.post('/api/timeline',requireUser,(req,res)=>{const text=clean(req.body.text);const media=base64(req.body.media);if(!text&&!media)return res.status(400).json({message:'Write something or add a photo/video.'});if(media && !/^data:(image|video)\//.test(media))return res.status(400).json({message:'Only image or video media is allowed.'});const pid=id('post');const type=clean(req.body.mediaType)||((media.match(/^data:([^;]+)/)||[])[1]||'');db.prepare('INSERT INTO timeline_posts(public_id,user_id,username,fullname,text,media_data,media_type,status) VALUES(?,?,?,?,?,?,?,?)').run(pid,req.user.brave_id,req.user.username,req.user.fullname,text,media,type,'pending');db.prepare('INSERT INTO moderation(public_id,content_type,content_id,reason) VALUES(?,?,?,?)').run(id('moderation'),'timeline',pid,'New timeline submission');res.status(201).json({message:'Posted to Timeline.',post:db.prepare('SELECT * FROM timeline_posts WHERE public_id=?').get(pid)});});
app.post('/api/timeline/:id/view',(req,res)=>{db.prepare('UPDATE timeline_posts SET views=views+1 WHERE public_id=?').run(req.params.id);res.json({ok:true});});

app.get('/api/search',(req,res)=>{const q=clean(req.query.q).toLowerCase();if(!q)return res.json({products:[],services:[],users:[]});const like='%'+q+'%';res.json({products:db.prepare("SELECT public_id,name,category,description,owner_name,owner_username,price,featured FROM products WHERE status='active' AND (lower(name) LIKE ? OR lower(category) LIKE ? OR lower(description) LIKE ?) ORDER BY featured DESC,id DESC LIMIT 30").all(like,like,like),services:db.prepare("SELECT public_id,name,category,description,owner_name,owner_username,price FROM services WHERE status='active' AND (lower(name) LIKE ? OR lower(category) LIKE ? OR lower(description) LIKE ?) ORDER BY id DESC LIMIT 30").all(like,like,like),users:db.prepare("SELECT username,fullname,country FROM users WHERE account_status='active' AND (lower(username) LIKE ? OR lower(fullname) LIKE ?) LIMIT 30").all(like,like)});});

app.get('/api/profile/:username',(req,res)=>{const u=db.prepare('SELECT * FROM users WHERE username=? AND account_status=\'active\'').get(username(req.params.username));if(!u)return res.status(404).json({message:'Profile not found.'});const posts=db.prepare("SELECT * FROM timeline_posts WHERE user_id=? AND status='active' ORDER BY id DESC LIMIT 50").all(u.brave_id);const products=db.prepare("SELECT * FROM products WHERE owner_id=? AND status='active' ORDER BY featured DESC,id DESC LIMIT 50").all(u.brave_id);const services=db.prepare("SELECT * FROM services WHERE owner_id=? AND status='active' ORDER BY id DESC LIMIT 50").all(u.brave_id);res.json({user:safeUser(u),profileUrl:publicUrl('/u/'+u.username),posts,products,services});});

app.post('/api/records',requireUser,(req,res)=>{const rid=id('record');db.prepare('INSERT INTO records(public_id,user_id,record_type,title,details,amount,reference) VALUES(?,?,?,?,?,?,?)').run(rid,req.user.brave_id,clean(req.body.recordType)||'general',clean(req.body.title),clean(req.body.details),req.body.amount===''?null:Number(req.body.amount)||null,clean(req.body.reference));res.status(201).json({message:'Record saved.',record:db.prepare('SELECT * FROM records WHERE public_id=?').get(rid)});});
app.get('/api/records',requireUser,(req,res)=>res.json({records:db.prepare('SELECT * FROM records WHERE user_id=? ORDER BY id DESC').all(req.user.brave_id)}));

app.post('/api/receipts',requireUser,(req,res)=>{const rid=id('receipt');const ref=clean(req.body.reference)||'BRV-'+Date.now().toString(36).toUpperCase();db.prepare('INSERT INTO receipts(public_id,user_id,type,sender,recipient,reference,work,amount,delivery,payment_status) VALUES(?,?,?,?,?,?,?,?,?,?)').run(rid,req.user.brave_id,clean(req.body.type)||'Sales Receipt',clean(req.body.sender),clean(req.body.recipient),ref,clean(req.body.work),req.body.amount===''?null:Number(req.body.amount)||null,req.body.delivery===''?null:Number(req.body.delivery)||null,clean(req.body.paymentStatus)||'Payment not enabled — record only');res.status(201).json({message:'Receipt saved.',receipt:db.prepare('SELECT * FROM receipts WHERE public_id=?').get(rid)});});
app.get('/api/receipts',requireUser,(req,res)=>res.json({receipts:db.prepare('SELECT * FROM receipts WHERE user_id=? ORDER BY id DESC').all(req.user.brave_id)}));

app.post('/api/customers',requireUser,(req,res)=>{const cid=id('customer');db.prepare('INSERT INTO customers(public_id,user_id,name,contact,notes) VALUES(?,?,?,?,?)').run(cid,req.user.brave_id,clean(req.body.name),clean(req.body.contact),clean(req.body.notes));res.status(201).json({message:'Customer saved.'});});
app.get('/api/customers',requireUser,(req,res)=>res.json({customers:db.prepare('SELECT * FROM customers WHERE user_id=? ORDER BY id DESC').all(req.user.brave_id)}));

app.post('/api/businesses',requireUser,(req,res)=>{const bid=id('business');db.prepare('INSERT INTO businesses(public_id,owner_id,username,name,category,description,phone,email,address,website,logo_data) VALUES(?,?,?,?,?,?,?,?,?,?,?)').run(bid,req.user.brave_id,req.user.username,clean(req.body.name),clean(req.body.category),clean(req.body.description),clean(req.body.phone),clean(req.body.email),clean(req.body.address),clean(req.body.website),base64(req.body.logo));res.status(201).json({message:'Business profile saved.',business:db.prepare('SELECT * FROM businesses WHERE public_id=?').get(bid)});});
app.get('/api/businesses/mine',requireUser,(req,res)=>res.json({businesses:db.prepare('SELECT * FROM businesses WHERE owner_id=? ORDER BY id DESC').all(req.user.brave_id)}));

app.post('/api/messages',requireUser,(req,res)=>{const to=clean(req.body.receiverId), msg=clean(req.body.message);if(!to||!msg)return res.status(400).json({message:'Recipient and message are required.'});db.prepare('INSERT INTO messages(public_id,sender_id,receiver_id,message) VALUES(?,?,?,?)').run(id('msg'),req.user.brave_id,to,msg);db.prepare('INSERT INTO notifications(public_id,user_id,title,message) VALUES(?,?,?,?)').run(id('note'),to,'New BRAVE message',`You received a new message from ${req.user.fullname}.`);res.status(201).json({message:'Message sent.'});});
app.get('/api/messages',requireUser,(req,res)=>res.json({messages:db.prepare('SELECT * FROM messages WHERE sender_id=? OR receiver_id=? ORDER BY id DESC').all(req.user.brave_id,req.user.brave_id)}));
app.get('/api/notifications',requireUser,(req,res)=>res.json({notifications:db.prepare('SELECT * FROM notifications WHERE user_id=? ORDER BY id DESC LIMIT 50').all(req.user.brave_id)}));
app.post('/api/notifications/:id/read',requireUser,(req,res)=>{db.prepare('UPDATE notifications SET is_read=1 WHERE public_id=? AND user_id=?').run(req.params.id,req.user.brave_id);res.json({message:'Notification marked as read.'});});

app.post('/api/advertising-requests',requireUser,(req,res)=>{const rid=id('ad');db.prepare('INSERT INTO advertising_requests(public_id,user_id,title,category,description,contact) VALUES(?,?,?,?,?,?)').run(rid,req.user.brave_id,clean(req.body.title),clean(req.body.category),clean(req.body.description),clean(req.body.contact));res.status(201).json({message:'Advertising request submitted.'});});
app.get('/api/advertising-requests/mine',requireUser,(req,res)=>res.json({requests:db.prepare('SELECT * FROM advertising_requests WHERE user_id=? ORDER BY id DESC').all(req.user.brave_id)}));
app.post('/api/reports',requireUser,(req,res)=>{const rid=id('report');db.prepare('INSERT INTO reports(public_id,reporter_id,content_type,content_id,reason) VALUES(?,?,?,?,?)').run(rid,req.user.brave_id,clean(req.body.contentType),clean(req.body.contentId),clean(req.body.reason));security(req,'content_report',req.user.brave_id,clean(req.body.reason),5);res.status(201).json({message:'Report sent to admin for review.'});});

// Reviews, flags and moderation.
app.get('/api/reviews', (req,res)=>{
 const product=clean(req.query.product), service=clean(req.query.service), user=clean(req.query.user);
 let q="SELECT r.*,u.username AS reviewer_username,u.fullname AS reviewer_name FROM reviews r LEFT JOIN users u ON u.brave_id=r.reviewer_id WHERE r.status='approved'"; const a=[];
 if(product){q+=' AND r.product_id=?';a.push(product)} else if(service){q+=' AND r.service_id=?';a.push(service)} else if(user){q+=' AND r.reviewed_user_id=?';a.push(user)} else return res.json({reviews:[]});
 res.json({reviews:db.prepare(q+' ORDER BY r.id DESC').all(...a)});
});
app.post('/api/reviews',requireUser,(req,res)=>{
 const rating=Number(req.body.rating), text=clean(req.body.reviewText), product=clean(req.body.productId), service=clean(req.body.serviceId);
 if(!Number.isInteger(rating)||rating<1||rating>5||(!product&&!service)||!text)return res.status(400).json({message:'A 1–5 rating, review text and product/service are required.'});
 let target=product?db.prepare("SELECT owner_id FROM products WHERE public_id=? AND status='active'").get(product):db.prepare("SELECT owner_id FROM services WHERE public_id=? AND status='active'").get(service);
 if(!target)return res.status(404).json({message:'Listing not found.'});
 if(target.owner_id===req.user.brave_id)return res.status(400).json({message:'You cannot review your own listing.'});
 const rid=id('review'); db.prepare('INSERT INTO reviews(public_id,reviewer_id,reviewed_user_id,product_id,service_id,rating,review_text) VALUES(?,?,?,?,?,?,?)').run(rid,req.user.brave_id,target.owner_id,product||null,service||null,rating,text);
 res.status(201).json({message:'Review submitted.',review:db.prepare('SELECT * FROM reviews WHERE public_id=?').get(rid)});
});
app.post('/api/reviews/:id/flag',requireUser,(req,res)=>{
 const r=db.prepare('SELECT * FROM reviews WHERE public_id=?').get(req.params.id); const reason=clean(req.body.reason);
 if(!r||!reason)return res.status(400).json({message:'Review and reason are required.'});
 const fid=id('reviewflag'); db.prepare('INSERT INTO review_flags(public_id,review_id,reporter_id,reason) VALUES(?,?,?,?)').run(fid,r.id,req.user.brave_id,reason); db.prepare("UPDATE reviews SET flag_status='flagged' WHERE id=?").run(r.id); security(req,'review_flagged',req.user.brave_id,reason,5); res.status(201).json({message:'Review flagged for administrative review.'});
});
app.get('/api/admin/reviews',requireAdmin,(req,res)=>res.json({reviews:db.prepare('SELECT r.*,u.username AS reviewer_username,u.fullname AS reviewer_name,v.username AS reviewed_username FROM reviews r LEFT JOIN users u ON u.brave_id=r.reviewer_id LEFT JOIN users v ON v.brave_id=r.reviewed_user_id ORDER BY r.id DESC').all()}));
app.get('/api/admin/reviews/flagged',requireAdmin,(req,res)=>res.json({reviews:db.prepare("SELECT r.*,u.username AS reviewer_username,v.username AS reviewed_username,f.reason AS flag_reason,f.status AS flag_queue_status FROM reviews r LEFT JOIN users u ON u.brave_id=r.reviewer_id LEFT JOIN users v ON v.brave_id=r.reviewed_user_id LEFT JOIN review_flags f ON f.review_id=r.id WHERE r.flag_status='flagged' ORDER BY r.id DESC").all()}));
app.post('/api/admin/reviews/:id/action',requireAdmin,(req,res)=>{const a=clean(req.body.action),r=db.prepare('SELECT * FROM reviews WHERE public_id=?').get(req.params.id);if(!r)return res.status(404).json({message:'Review not found.'});if(!['reviewed','hide','restore','investigate'].includes(a))return res.status(400).json({message:'Invalid review action.'});const status=a==='hide'?'hidden':a==='restore'?'approved':r.status;const flag=a==='restore'?'none':a==='investigate'?'flagged':r.flag_status;db.prepare('UPDATE reviews SET status=?,flag_status=?,updated_at=? WHERE public_id=?').run(status,flag,now(),r.public_id);db.prepare("UPDATE review_flags SET status=? WHERE review_id=?").run(a==='hide'?'actioned':a==='restore'?'resolved':'reviewed',r.id);audit('review_'+a,'review',r.public_id,clean(req.body.note));res.json({message:'Review action completed.'});});
app.get('/api/admin/moderation',requireAdmin,(req,res)=>res.json({items:db.prepare('SELECT * FROM moderation ORDER BY status ASC,id DESC').all()}));
app.post('/api/admin/moderation/:id/action',requireAdmin,(req,res)=>{const a=clean(req.body.action),m=db.prepare('SELECT * FROM moderation WHERE public_id=?').get(req.params.id);if(!m)return res.status(404).json({message:'Moderation item not found.'});const status={approve:'approved',reject:'rejected',flag:'flagged'}[a];if(!status)return res.status(400).json({message:'Invalid moderation action.'});db.prepare('UPDATE moderation SET status=?,admin_id=?,notes=?,updated_at=? WHERE public_id=?').run(status,'ADMIN',clean(req.body.note),now(),m.public_id);const table=m.content_type==='product'?'products':m.content_type==='service'?'services':m.content_type==='timeline'?'timeline_posts':null;if(table){const target=status==='approved'?'active':status==='rejected'?'removed':status;db.prepare(`UPDATE ${table} SET status=? WHERE public_id=?`).run(target,m.content_id);}audit('moderation_'+a,m.content_type,m.content_id,clean(req.body.note));res.json({message:'Moderation action completed.'});});

app.post('/api/ai/chat',(req,res)=>{const text=clean(req.body.message);if(!text)return res.status(400).json({message:'Please enter a message.'});const locale=['ng','gb','us'].includes(req.body.locale)?req.body.locale:'ng';const lower=text.toLowerCase();let reply;
 const p=db.prepare("SELECT * FROM products WHERE status='active' AND (lower(name) LIKE ? OR lower(category) LIKE ? OR lower(description) LIKE ?) ORDER BY featured DESC,id DESC LIMIT 5").all('%'+lower+'%','%'+lower+'%','%'+lower+'%');
 const s=db.prepare("SELECT * FROM services WHERE status='active' AND (lower(name) LIKE ? OR lower(category) LIKE ? OR lower(description) LIKE ?) ORDER BY id DESC LIMIT 5").all('%'+lower+'%','%'+lower+'%','%'+lower+'%');
 if(p.length) reply=`I found ${p.length} product option${p.length===1?'':'s'} that match your search. ${p.slice(0,3).map(x=>x.name).join(', ')}.`; else if(s.length) reply=`I found ${s.length} service option${s.length===1?'':'s'} that may match. ${s.slice(0,3).map(x=>x.name).join(', ')}.`; else {const list=aiResponses[locale];reply=list[Math.floor(Math.random()*list.length)];}
 const cid=id('ai');db.prepare('INSERT INTO ai_conversations(public_id,user_id,message,reply,locale) VALUES(?,?,?,?,?)').run(cid,clean(req.body.userId)||null,text,reply,locale);res.json({reply,products:p,services:s,conversationId:cid});
});

// Optional phone verification. No Twilio credentials are required for registration; when absent these routes explain that verification is optional.
app.post('/api/phone/send-otp',(req,res)=>res.json({message:'Phone verification is currently optional because Twilio is not configured.',optional:true,channel:clean(req.body.channel)||'sms'}));
app.post('/api/phone/verify',(req,res)=>res.json({message:'Phone verification is currently optional because Twilio is not configured.',verified:false,optional:true}));

// Password reset by email. Tokens are persisted as hashes so resets survive a server restart.
function tokenHash(t){return crypto.createHash('sha256').update(String(t)).digest('hex');}
app.post('/forgot-password',async(req,res)=>{
 const em=email(req.body.email); const u=db.prepare('SELECT * FROM users WHERE email=?').get(em);
 try{
  if(u){
   db.prepare('UPDATE reset_tokens SET used_at=? WHERE user_id=? AND used_at IS NULL').run(now(),u.brave_id);
   const raw=crypto.randomBytes(32).toString('hex');
   db.prepare('INSERT INTO reset_tokens(token_hash,user_id,expires_at) VALUES(?,?,?)').run(tokenHash(raw),u.brave_id,new Date(Date.now()+30*60*1000).toISOString());
   const url=publicUrl('/reset-password.html?token='+encodeURIComponent(raw));
   const sent=await sendMail(em,'Reset your Unique BRAVE password',`<p>Hello ${clean(u.fullname)},</p><p>Use the button below to reset your password. This link expires in 30 minutes.</p><p><a href="${url}">Reset my BRAVE password</a></p>`);
   if(!sent) console.error('Password recovery email was not sent; check SMTP configuration.');
  }
 }catch(e){console.error('Password recovery failure:',e.message)}
 res.json({message:'If that email belongs to a BRAVE account, a reset link has been sent.'});
});
app.post('/reset-password',async(req,res)=>{
 const token=clean(req.body.token),pw=String(req.body.password||'');
 if(!token||pw.length<6)return res.status(400).json({message:'This reset link is invalid or the password is too short.'});
 const r=db.prepare("SELECT * FROM reset_tokens WHERE token_hash=? AND used_at IS NULL AND expires_at>? ORDER BY id DESC LIMIT 1").get(tokenHash(token),new Date().toISOString());
 if(!r)return res.status(400).json({message:'This reset link is invalid or expired.'});
 db.prepare('UPDATE users SET password_hash=?,updated_at=? WHERE brave_id=?').run(await bcrypt.hash(pw,10),now(),r.user_id);
 db.prepare('UPDATE reset_tokens SET used_at=? WHERE id=?').run(now(),r.id);
 security(req,'password_reset',r.user_id,'Password reset completed',0);
 res.json({message:'Password reset successfully.'});
});

// Admin
app.post('/admin-login',(req,res)=>{const ok=email(req.body.email)===email(process.env.ADMIN_EMAIL||'') && String(req.body.password||'')===String(process.env.ADMIN_PASSWORD||'');if(!ok){security(req,'failed_admin_login','','Invalid admin credentials',70);return res.status(401).json({message:'Invalid administrator credentials.'});}const token=crypto.randomBytes(32).toString('hex');adminSessions.set(token,{createdAt:Date.now()});audit('admin_login');res.json({message:'Administrator login successful.',token});});
app.post('/api/admin/logout',requireAdmin,(req,res)=>{adminSessions.delete(req.headers['x-admin-token']);res.json({message:'Administrator logged out.'});});
app.get('/api/admin/overview',requireAdmin,(req,res)=>res.json({users:db.prepare('SELECT COUNT(*) c FROM users').get().c,products:db.prepare("SELECT COUNT(*) c FROM products WHERE status='active'").get().c,services:db.prepare("SELECT COUNT(*) c FROM services WHERE status='active'").get().c,posts:db.prepare("SELECT COUNT(*) c FROM timeline_posts WHERE status='active'").get().c,pendingRequests:db.prepare("SELECT COUNT(*) c FROM protected_requests WHERE status='pending'").get().c,pendingReports:db.prepare("SELECT COUNT(*) c FROM reports WHERE status='pending'").get().c,suspicious:db.prepare("SELECT COUNT(*) c FROM security_events WHERE status='review' AND risk_score>=40").get().c,aiConversations:db.prepare('SELECT COUNT(*) c FROM ai_conversations').get().c}));
app.get('/api/admin/users',requireAdmin,(req,res)=>res.json({users:db.prepare('SELECT * FROM users ORDER BY id DESC').all().map(safeUser)}));
app.get('/api/admin/users/:id',requireAdmin,(req,res)=>{const u=db.prepare('SELECT * FROM users WHERE brave_id=?').get(req.params.id);if(!u)return res.status(404).json({message:'User not found.'});res.json({user:safeUser(u),posts:db.prepare('SELECT * FROM timeline_posts WHERE user_id=? ORDER BY id DESC').all(u.brave_id),products:db.prepare('SELECT * FROM products WHERE owner_id=? ORDER BY id DESC').all(u.brave_id),services:db.prepare('SELECT * FROM services WHERE owner_id=? ORDER BY id DESC').all(u.brave_id)});});
app.post('/api/admin/users/:id/action',requireAdmin,(req,res)=>{const action=clean(req.body.action);const u=db.prepare('SELECT * FROM users WHERE brave_id=?').get(req.params.id);if(!u)return res.status(404).json({message:'User not found.'});let status=u.account_status;if(action==='suspend')status='suspended';else if(action==='restrict')status='restricted';else if(action==='restore')status='active';else return res.status(400).json({message:'Invalid user action.'});db.prepare('UPDATE users SET account_status=?,updated_at=? WHERE brave_id=?').run(status,now(),u.brave_id);audit('user_'+action,'user',u.brave_id);res.json({message:'User status updated.',user:safeUser(db.prepare('SELECT * FROM users WHERE brave_id=?').get(u.brave_id))});});
app.get('/api/admin/products',requireAdmin,(req,res)=>res.json({products:db.prepare('SELECT * FROM products ORDER BY featured DESC,id DESC').all()}));
app.get('/api/admin/services',requireAdmin,(req,res)=>res.json({services:db.prepare('SELECT * FROM services ORDER BY id DESC').all()}));
app.post('/api/admin/content/:id/action',requireAdmin,(req,res)=>{const type=clean(req.body.contentType),action=clean(req.body.action),table=type==='product'?'products':type==='service'?'services':null;if(!table)return res.status(400).json({message:'Invalid content type.'});const row=db.prepare(`SELECT * FROM ${table} WHERE public_id=?`).get(req.params.id);if(!row)return res.status(404).json({message:'Content not found.'});if(action==='approve')db.prepare(`UPDATE ${table} SET status='active' WHERE public_id=?`).run(req.params.id);else if(action==='remove')db.prepare(`UPDATE ${table} SET status='removed' WHERE public_id=?`).run(req.params.id);else if(action==='feature'&&table==='products')db.prepare('UPDATE products SET featured=1 WHERE public_id=?').run(req.params.id);else if(action==='unfeature'&&table==='products')db.prepare('UPDATE products SET featured=0 WHERE public_id=?').run(req.params.id);else return res.status(400).json({message:'Invalid content action.'});audit('content_'+action,type,req.params.id);res.json({message:'Content action completed.'});});
app.get('/api/admin/protected-requests',requireAdmin,(req,res)=>res.json({requests:db.prepare('SELECT r.*,u.fullname,u.username,u.email,u.phone FROM protected_requests r LEFT JOIN users u ON u.brave_id=r.user_id ORDER BY r.status ASC,r.id DESC').all()}));
app.post('/api/admin/protected-requests/:id/action',requireAdmin,(req,res)=>{const r=db.prepare('SELECT * FROM protected_requests WHERE public_id=?').get(req.params.id);if(!r)return res.status(404).json({message:'Request not found.'});const action=clean(req.body.action),status=action==='approve'?'approved':action==='reject'?'rejected':null;if(!status)return res.status(400).json({message:'Invalid request action.'});if(status==='approved'){const u=db.prepare('SELECT * FROM users WHERE brave_id=?').get(r.user_id);if(r.type==='phone number'&&r.requested_value)db.prepare('UPDATE users SET phone=?,updated_at=? WHERE brave_id=?').run(phone(r.requested_value),now(),u.brave_id);if(r.type==='email address'&&r.requested_value)db.prepare('UPDATE users SET email=?,updated_at=? WHERE brave_id=?').run(email(r.requested_value),now(),u.brave_id);if(r.type==='profile image'&&r.requested_value)db.prepare('UPDATE users SET profile_image=?,updated_at=? WHERE brave_id=?').run(base64(r.requested_value),now(),u.brave_id);if(r.type==='verification method')db.prepare('UPDATE users SET verification_status=\'verified\',updated_at=? WHERE brave_id=?').run(now(),u.brave_id);if(r.type==='account deletion')db.prepare('UPDATE users SET account_status=\'deleted\',updated_at=? WHERE brave_id=?').run(now(),u.brave_id);}db.prepare('UPDATE protected_requests SET status=?,admin_note=?,updated_at=? WHERE public_id=?').run(status,clean(req.body.note),now(),r.public_id);audit('protected_request_'+status,'request',r.public_id,r.type);res.json({message:'Request updated.'});});
app.get('/api/admin/reports',requireAdmin,(req,res)=>res.json({reports:db.prepare('SELECT r.*,u.fullname,u.username FROM reports r LEFT JOIN users u ON u.brave_id=r.reporter_id ORDER BY r.status ASC,r.id DESC').all()}));
app.post('/api/admin/reports/:id/action',requireAdmin,(req,res)=>{const a=clean(req.body.action);if(!['reviewed','dismissed','actioned'].includes(a))return res.status(400).json({message:'Invalid report action.'});db.prepare('UPDATE reports SET status=?,admin_note=? WHERE public_id=?').run(a,clean(req.body.note),req.params.id);audit('report_'+a,'report',req.params.id);res.json({message:'Report updated.'});});
app.get('/api/admin/security',requireAdmin,(req,res)=>res.json({events:db.prepare('SELECT s.*,u.fullname,u.username,u.email FROM security_events s LEFT JOIN users u ON u.brave_id=s.user_id ORDER BY s.id DESC LIMIT 200').all()}));
app.post('/api/admin/security/:id/action',requireAdmin,(req,res)=>{const a=clean(req.body.action);if(!['reviewed','cleared'].includes(a))return res.status(400).json({message:'Invalid security action.'});db.prepare('UPDATE security_events SET status=? WHERE public_id=?').run(a,req.params.id);audit('security_'+a,'security',req.params.id);res.json({message:'Security event updated.'});});
app.get('/api/admin/activity',requireAdmin,(req,res)=>res.json({activity:db.prepare('SELECT * FROM admin_audit ORDER BY id DESC LIMIT 200').all()}));
app.get('/api/admin/ai-conversations',requireAdmin,(req,res)=>res.json({conversations:db.prepare('SELECT * FROM ai_conversations ORDER BY id DESC LIMIT 200').all()}));
app.get('/api/admin/advertising-requests',requireAdmin,(req,res)=>res.json({requests:db.prepare('SELECT a.*,u.fullname,u.username,u.email FROM advertising_requests a LEFT JOIN users u ON u.brave_id=a.user_id ORDER BY a.status ASC,a.id DESC').all()}));
app.post('/api/admin/advertising-requests/:id/action',requireAdmin,(req,res)=>{const a=clean(req.body.action),status={approve:'approved',reject:'rejected',pause:'paused',complete:'completed'}[a];if(!status)return res.status(400).json({message:'Invalid advertising action.'});db.prepare('UPDATE advertising_requests SET status=?,updated_at=? WHERE public_id=?').run(status,now(),req.params.id);audit('advertising_'+status,'advertising',req.params.id);res.json({message:'Advertising request updated.'});});
app.get('/api/admin/verifications',requireAdmin,(req,res)=>res.json({users:db.prepare("SELECT * FROM users WHERE verification_status!='verified' ORDER BY id DESC").all().map(safeUser)}));
app.post('/api/admin/verifications/:id/action',requireAdmin,(req,res)=>{const a=clean(req.body.action);if(!['approve','reject'].includes(a))return res.status(400).json({message:'Invalid verification action.'});db.prepare('UPDATE users SET verification_status=?,email_verified=?,phone_verified=?,updated_at=? WHERE brave_id=?').run(a==='approve'?'verified':'rejected',a==='approve'?1:0,a==='approve'?1:0,now(),req.params.id);audit('verification_'+a,'user',req.params.id);res.json({message:'Verification status updated.'});});
app.get('/api/admin/companies',requireAdmin,(req,res)=>res.json({businesses:db.prepare('SELECT b.*,u.fullname,u.email FROM businesses b LEFT JOIN users u ON u.brave_id=b.owner_id ORDER BY b.id DESC').all()}));

// Compatibility endpoints used by older frontend versions.
app.get('/api/admin/transactions',requireAdmin,(req,res)=>res.json({transactions:[]}));
app.get('/api/admin/receipts',requireAdmin,(req,res)=>res.json({receipts:db.prepare('SELECT * FROM receipts ORDER BY id DESC LIMIT 200').all()}));
app.get('/api/admin/refunds',requireAdmin,(req,res)=>res.json({refunds:[]}));
app.get('/api/admin/users/:id/timeline',requireAdmin,(req,res)=>res.json(db.prepare('SELECT * FROM timeline_posts WHERE user_id=? ORDER BY id DESC').all(req.params.id)));
app.get('/api/brave/faq',(req,res)=>res.json({faq:allResponses.map((answer,i)=>({question:'BRAVE AI response '+(i+1),answer}))}));

app.use((req,res,next)=>{if(req.path.startsWith('/api/')||req.path==='/login'||req.path==='/register')return res.status(404).json({message:'API route not found.'});next();});
app.listen(PORT,()=>console.log(`BRAVE COM backend is running on port ${PORT}`));
